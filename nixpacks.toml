[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd Backend && npm install"]

[phases.build]
cmds = ["# Skipping frontend build for backend-only deployment"]

[start]
cmd = "cd Backend && npm start"
